import React from "react";

const Loading = () => {
    return (
        <div className = "loading-main">
            <h2 className = "loading-message">
                Loading...
            </h2>
        </div>
    );
};

export default Loading;